filter {
    if [type] == "rsyslog_client" {
        if [message] =~ "pihole" {
            mutate {
                add_tag => [ "pihole" ]
            }
        } else if [message] =~ "openvpn" {
            mutate {
                add_tag => [ "openvpn" ]
            }
        }
    }
}
